package com.yamilov.raiffeisen.devportal;

import com.codeborne.selenide.Configuration;
import org.junit.jupiter.api.Test;
import org.openqa.selenium.By;

import static com.codeborne.selenide.Selenide.*;

public class RaifTest {/**
    @Test
    public void firstTest() {
        Configuration.timeout = 9000;  // global timeout
        Configuration.browser = "chrome";
//        Configuration.browser = "safari";
//        Configuration.startMaximized = true;  // стартануть в максимальном режиме. Не рекомендуется
//        Configuration.browserSize = "800x600";
//        Configuration.browserSize = "1920x1080";
        Configuration.browserSize = "1280x720";
        open("https://api.raiffeisen.ru/");
        $(By.xpath("//*[contains(text(), 'Посмотреть API продукты')]")).click();
        sleep(3000);
    }
**/}
